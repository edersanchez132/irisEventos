export default class ApiAuth{

    getToken(){
      const ACCESS =  localStorage.getItem('token')
    }
    decodeToken = () => {
        try {
            
        } catch (error) {
            
        }
    }
}